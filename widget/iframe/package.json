{
  "name": "@arthur2079/widget-iframe",
  "version": "1.0.0",
  "license": "MIT",
  "private": true,
  "source": "src/index.ts",
  "browserslist": "> 0.5%, last 2 versions, not dead",
  "scripts": {
    "build": "parcel build --no-source-maps --cache-dir=.parcel-cache",
    "clean": "rimraf .parcel-cache && rimraf dist",
    "format": "prettier --write '{.,src}/**/*.{ts,tsx}'",
    "lint": "eslint \"**/*.{ts,tsx}\""
  },
  "devDependencies": {
    "parcel": "^2.16.4"
  },
  "dependencies": {
    "@arthur2079/widget-embedded": "^1.0.0"
  }
}
